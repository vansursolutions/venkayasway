# venkayaswamy.com — Sri Venkaiah Swamy Temple, Sullurupeta

A simple bilingual (English / Telugu) website that also installs as a mobile app (PWA) and
ships as an Android app (Capacitor). No build step: the site is plain HTML, CSS and JS in `www/`.

## Folder layout

```
www/                 the website (this folder is deployed as-is)
  index.html         home
  life.html          life story
  teachings.html     teachings
  temple.html        timings, festivals, address, directions
  gallery.html       photos and videos
  donate.html        donations and contact form
  css/style.css      styles
  js/site.js         header, footer, language toggle, PWA install
  images/            photos (replace the placeholders)
  icons/             app icons (regenerate with `npm run icons`, or drop in your own)
  manifest.webmanifest, sw.js   PWA files
android/             Android project generated by Capacitor
tools/make-images.py generates placeholder images and icons
```

## Editing content

Every piece of text appears twice, wrapped in `<span class="en">…</span>` and `<span class="te">…</span>`.
Only the chosen language is shown; the toggle in the header switches between them.

Search the pages for `TODO` — those are placeholders that need real information:
temple address and phone, timings, bank / UPI details, Formspree form id, photos, video ids.

## Run locally

```
npm run dev          # then open http://localhost:3000
```

## Deploy the website (free hosting)

Any static host works. Recommended: Cloudflare Pages or Netlify.

1. Push this folder to a GitHub repository.
2. In Cloudflare Pages (or Netlify) create a project from that repo.
   Build command: none. Output / publish directory: `www`.
3. Add the custom domain `venkayaswamy.com` in the host's dashboard.
4. At your domain registrar, set the DNS records the host shows you
   (usually a CNAME for `www` and an A / ALIAS record for the root).
5. HTTPS is issued automatically. The site is then installable as an app on Android and iOS
   ("Add to Home Screen").

When you change files, bump `CACHE` in `www/sw.js` (v1 → v2) so phones fetch the new version.

## Android app (Play Store)

One-time setup:
1. Install Android Studio from https://developer.android.com/studio (includes the SDK).
2. Open Android Studio once and let it finish installing the SDK.

Build and run:
```
npm run android:sync     # copy www/ into the Android project (run after every site change)
npm run android:open     # opens android/ in Android Studio; press Run to test on a phone/emulator
```

Publish:
1. In Android Studio: Build → Generate Signed Bundle / APK → Android App Bundle. Create a keystore
   and keep it safe (you need the same keystore for every future update).
2. Create a developer account at https://play.google.com/console (one-time fee).
3. Create the app, upload the `.aab`, fill in the store listing (screenshots, description, privacy
   policy URL — a page on venkayaswamy.com is fine), and submit for review.

App id: `com.srivenkaiahswamy.app` (in `capacitor.config.json`). App name and icon: `android/app/src/main/res/`.
To use your own icon in the app, replace the files under `android/app/src/main/res/mipmap-*`
or use Android Studio: right-click `res` → New → Image Asset.

## AWS hosting (production: venkayaswamy.com)

- S3 bucket `venkayaswamy-site-030011113848` (private) holds the files in `www/`.
- CloudFront distribution `E1H9ES2VPQGWUO` serves them over HTTPS (`d1h9gkukue41oc.cloudfront.net`).
- ACM certificate for venkayaswamy.com + www (us-east-1).
- Route 53 hosted zone for venkayaswamy.com holds the DNS records. The domain registrar must
  point the domain at the Route 53 name servers (see the zone in the AWS console).

Deploy: commit on main, then `./deploy.sh` (needs the AWS CLI logged in). It pushes to GitHub,
syncs `www/` to S3 and invalidates the CloudFront cache.

## Backend (admin, uploads, events, annadanam sponsors)

Serverless, in the same AWS account, CloudFormation stack `venkaiahswamy` (template generated by
`infra/gen_template.py`, Lambda code in `backend/app.py`):

- **Cognito user pool** for logins. Groups: `admin` (everything) and `member` (upload photos/videos only).
  Admins add users from the admin page; the person gets a temporary password by email.
- **HTTP API + Lambda** (`venkaiahswamy-api`): events, media, sponsors, users.
- **DynamoDB** tables `venkaiahswamy-events`, `-sponsors`, `-media`.
- **S3 media bucket** `venkaiahswamy-media-…` served through CloudFront at `/media/…`.
- **SES** sends sponsor confirmations from `noreply@srivenkaiahswamy.com` and notifies the admin email.
- **SNS** sends the SMS confirmation. The account is in the SNS SMS sandbox until production access is
  requested; India also needs a registered sender ID (DLT) for reliable delivery.

Admin page: https://srivenkaiahswamy.com/admin/

Redeploy the Lambda after editing `backend/app.py`:
```
cd backend && zip -q ../infra/api.zip app.py && cd .. && \
aws lambda update-function-code --function-name venkaiahswamy-api --zip-file fileb://infra/api.zip
```
Change infrastructure: edit `infra/gen_template.py`, run it, then `aws cloudformation update-stack … --template-body file://infra/backend.json`.

## Publishing to Google Play from the command line

One-time: create a service account in Google Cloud, enable the "Google Play Android Developer API",
download its JSON key to `android/play-service-account.json` (gitignored), and invite the service
account's email in Play Console → Users and permissions with release and store-presence rights.

```
python3 android/publish.py status                                   # what is live
python3 android/publish.py listing                                  # push listing text + images from android/store
python3 android/publish.py upload android/store/<bundle>.aab        # upload + roll out to production
python3 android/publish.py upload <bundle>.aab --track internal     # or to internal testing first
```
Bump `versionCode` in `android/app/build.gradle` before each new bundle.
